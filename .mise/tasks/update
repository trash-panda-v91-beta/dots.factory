#!/usr/bin/env bash
#MISE description="Update all npins sources (or a specific pin)"

set -euo pipefail

pin="${1:-}"

if [ -n "$pin" ]; then
  nix run nixpkgs#npins -- update "$pin"
else
  nix run nixpkgs#npins -- update
fi
